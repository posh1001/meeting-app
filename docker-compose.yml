version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel-app
    working_dir: /var/www
    volumes:
      - ./:/var/www
    ports:
      - "8082:80"
    networks:
      - laravel-network

networks:
  laravel-network:
    driver: bridge

